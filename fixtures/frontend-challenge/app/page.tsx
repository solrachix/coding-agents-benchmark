"use client";
import { useEffect, useMemo, useState } from "react";

type EventItem = { id: number; title: string; category: string; city: string; date: string; price: string; color: string };
const events: EventItem[] = [
  { id: 1, title: "Designing for tomorrow", category: "Design", city: "Austin, TX", date: "May 22 · 7:00 PM", price: "$34", color: "coral" },
  { id: 2, title: "Night market sessions", category: "Food", city: "Brooklyn, NY", date: "May 25 · 6:30 PM", price: "$18", color: "violet" },
  { id: 3, title: "Indie makers meetup", category: "Community", city: "Portland, OR", date: "May 29 · 5:00 PM", price: "Free", color: "green" },
  { id: 4, title: "Future of product", category: "Business", city: "Chicago, IL", date: "Jun 02 · 8:00 AM", price: "$42", color: "blue" },
  { id: 5, title: "Open air cinema", category: "Arts", city: "Los Angeles, CA", date: "Jun 04 · 8:30 PM", price: "$12", color: "gold" },
  { id: 6, title: "Creative code lab", category: "Technology", city: "Seattle, WA", date: "Jun 08 · 4:00 PM", price: "$25", color: "pink" },
];
const categories = ["All events", "Design", "Food", "Community", "Business", "Arts"];

export default function Page() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All events"); const [list, setList] = useState(false); const [saved, setSaved] = useState<number[]>([]); const [selected, setSelected] = useState<EventItem | null>(null);
  useEffect(() => { const onKeyDown = (event: KeyboardEvent) => { if (event.key === "Escape") setSelected(null); }; window.addEventListener("keydown", onKeyDown); return () => window.removeEventListener("keydown", onKeyDown); }, []);
  const visible = useMemo(() => events.filter((event) => (category === "All events" || event.category === category) && event.title.toLowerCase().includes(query.toLowerCase())), [category, query]);
  return <main className="shell"><header className="topbar"><a className="brand" href="#content" aria-label="Event Atlas home"><span className="brand-mark">EA</span><span>Event Atlas</span></a><label className="search"><span className="sr-only">Search events</span><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search events" aria-label="Search events" /></label><button className="avatar" aria-label="Open profile menu">CM</button></header><div className="layout"><aside className="sidebar" aria-label="Main navigation"><p className="eyebrow">DISCOVER</p><a className="active" href="#content">Explore</a><a href="#saved">Saved events</a><a href="#calendar">Calendar</a><p className="eyebrow space">YOUR SPACE</p><a href="#settings">Settings</a></aside><section id="content" className="content"><div className="breadcrumbs">Home <span>/</span> Explore</div><div className="heading-row"><div><p className="eyebrow accent">CURATED FOR YOU</p><h1>Explore events</h1><p className="lede">Find something worth remembering.</p></div><button className="primary" onClick={() => setSelected(events[0])}>Create an event <span>+</span></button></div><nav className="filters" aria-label="Event categories">{categories.map((item) => <button key={item} className={category === item ? "chip selected" : "chip"} onClick={() => setCategory(item)} aria-pressed={category === item}>{item}</button>)}</nav><div className="toolbar"><p><strong>{visible.length}</strong> events near you</p><div className="view-tools"><button className={list ? "icon-button" : "icon-button selected-icon"} onClick={() => setList(false)} aria-label="Grid view">▦</button><button className={list ? "icon-button selected-icon" : "icon-button"} onClick={() => setList(true)} aria-label="List view">☷</button></div></div>{visible.length === 0 ? <div className="empty" role="status"><div className="empty-icon">⌕</div><h2>No events found</h2><p>Try another search or clear your filters.</p><button className="secondary" onClick={() => { setQuery(""); setCategory("All events"); }}>Clear filters</button></div> : <div className={list ? "event-grid list" : "event-grid"}>{visible.map((event) => <article className="event-card" key={event.id}><div className={`event-art ${event.color}`}><span>{event.category}</span></div><div className="event-body"><div className="event-title"><h2>{event.title}</h2><button className={saved.includes(event.id) ? "save saved" : "save"} onClick={() => setSaved((current) => current.includes(event.id) ? current.filter((id) => id !== event.id) : [...current, event.id])} aria-label={`Save ${event.title}`}>{saved.includes(event.id) ? "★" : "☆"}</button></div><p className="meta">{event.date}</p><p className="meta">{event.city}</p><div className="card-footer"><strong>{event.price}</strong><button className="text-button" onClick={() => setSelected(event)}>View details <span>→</span></button></div></div></article>)}</div>}<p className="result-note">Showing curated events · Updated today</p></section></div>{selected && <div className="modal-backdrop" role="presentation" onClick={() => setSelected(null)}><section className="modal" role="dialog" aria-modal="true" aria-labelledby="event-modal-title" onClick={(e) => e.stopPropagation()}><button className="modal-close" onClick={() => setSelected(null)} aria-label="Close details">×</button><div className={`event-art large ${selected.color}`} /><p className="eyebrow accent">{selected.category}</p><h2 id="event-modal-title">{selected.title}</h2><p className="meta">{selected.date} · {selected.city}</p><p>Join a thoughtful gathering of curious people and make an evening of it.</p><button className="primary full" onClick={() => setSelected(null)}>Get tickets</button></section></div>}</main>;
}
